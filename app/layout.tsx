import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'HomeCare Plus - Compassionate Care, Right at Home',
  description: 'Professional homecare services that bring comfort, dignity, and peace of mind to your family. 24/7 availability, certified caregivers, serving 3+ districts.',
  keywords: 'homecare, elderly care, medical assistance, caregivers, home health, senior care',
  generator: 'Next.js',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}